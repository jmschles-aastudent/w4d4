<% flash[:errors].try(:each) do |error| %>
	<%= error %>
<% end %>

<h3>Create New Band</h3>

<%= render :partial => "band_form", :locals => { :band => @band,
																								 :method => :post } %>